<% if @return_authorization.customer_shipments.any? %>
  <table class="table">
    <thead data-hook="customer_shipments_header">
      <tr>
        <th><%= Spree.t(:customer_shipments_number) %></th>
        <th><%= Spree.t(:customer_shipments_tracking) %></th>
        <th><%= Spree.t(:customer_shipments_weight) %></th>
        <th><%= "#{Spree.t('date')}/#{Spree.t('time')}" %></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
    <% @return_authorization.customer_shipments.each do |shipment| %>
        <tr id="<%= spree_dom_id(label) %>" data-hook="return_row">
          <td><%= shipment.number %></td>
          <td><%= link_to "#{shipment.tracking}", @label.tracking_label %></td>
          <td><%= shipment.weight %></td>
          <td><%= pretty_time(shipment.created_at) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

